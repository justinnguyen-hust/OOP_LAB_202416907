Trả lời câu hỏi:
- Nếu user chọn cancel, Output là "No"
- Để tùy chỉnh nút (option) trong JOptionPane, không dùng showConfirmDialog mà dùng JOptionPane.showOptionDialog(...)
  String[] options = {"Yes", "No"};
  
